# Sudoku Solver 使用说明

## 概述
此存储库包含一个用 C++ 实现的多线程数独求解器。该程序使用多个线程从文件中读取数独谜题，使用舞蹈链接算法求解，并输出结果。

## 环境要求
运行此代码之前，请确保已安装以下软件：
- C++ 编译器（如 g++、clang++ 等）
- C++ 标准库（大多数编译器都已包含）
- 支持线程、文件 I/O 和同步的标准库

## 环境设置

2. **编译代码**
   已经写好makefile文件，确保同时编译 `.cpp` 文件及其依赖项。可以使用以下命令进行编译：
   ```bash
   make
   ```

## 运行程序
1. **准备输入文件**
   
    创建包含数独谜题的文本文件。每行应包含一个数独谜题，长度为 N（通常为 81，即 9x9 数独）。
    创建包含数独谜题答案的文本文件，每行应包含一个数独谜题的答案，顺序应该与前面的输入文本文件相同
2. **运行程序**
   使用以下命令来运行程序，并提供输入文件名：
   ```bash
   ./sudoku_solver
   ``` 
   在程序运行时，您可以通过标准输入输入文件名。例如：
   ```bash
   test1
   test2
   ```
 3.**测试程序**
而要测试程序运行的速度，可以用所给的测试程序，按如下方式进行运行

```
./Lab1.sh test_group answer_group
   ```
这里test_group中需要包含数个已经写好数独谜题的文本文件，而answer_group需要保存与前面的相对应顺序的谜题答案的文件

## 程序输出
程序将在控制台输出每个求解的数独结果。每个结果将对应于输入的谜题，以字符串形式显示。输出格式通常为 N 个字符组成的字符串，表示数独的求解结果。


